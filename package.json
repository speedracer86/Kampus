{
  "name": "kampus-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Kampus — ADHD-first math practice for homeschooled kids, grades 3-8.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test -w @kampus/engine && npm run test -w @kampus/curriculum",
    "test:e2e": "node tests/test_kampus.js",
    "dev": "npm run dev -w @kampus/web",
    "build": "npm run build -w @kampus/web"
  },
  "devDependencies": {
    "playwright": "^1.45.0"
  }
}
